# Blender編集元の管理

- このリポジトリには `.blend` と `art/` 内のPNGを登録・アップロードしない。`.gitignore` を維持し、強制追加で回避しない。
- 編集元・検討画像はローカルと旧非公開リポジトリで保管する。サイト用 `.glb` と再生成用PythonスクリプトはGit管理する。
- 新しく手作業で編集した元データを保管する必要がある場合は、公開予定リポジトリへ混ぜず、非公開の保管先にバックアップする。

# Blender・画像の付加情報

- 生成スクリプトは保存前に `art/privacy.py` の `prepare_portable_scene` を使う。PNG等へのファイル名・ホスト名などのメタデータ記録を無効にし、ファイルブラウザーの保存ディレクトリを整理する。
- Blenderで手動保存すると、保存パス等が再び入る可能性がある。外部へ共有する場合は内部の付加情報を検査する。圧縮blendは全フレームを展開して確認する。
- レンダー画像への文字の焼き込みを無効にするだけでは、メタデータの除去確認の代わりにならない。
- 公開前には新しいファイルだけでなく公開対象のGit履歴も確認する。既に整理した未変更ファイルを毎回再加工する必要はない。

# FLOP Shrineのブランドカラー

- 基準色は **`#00B4D8`（RGB 0, 180, 216）**。ユーザー指定、2026-09-10再確認。
- 色はシアン。以前のエメラルドグリーンの仮配色に戻さない。
- 実装の基準値は `public/theme.json`。Webの材質・シェーダー・CSSとBlender生成スクリプトは、この値に合わせる。
- 明暗や淡いハイライトは基準色から派生させる。ユーザーの指示なしに基準色を変更しない。
